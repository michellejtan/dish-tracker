<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">

    <title>My Collection | DishNote</title>
</head>

<body>


    <%- include('../partials/_navbar.ejs') %>
        <%- include('../partials/_footer.ejs') %>
            <main>
                <h1 class="blur">Welcome to my Dishes Collection</h1>

                <a class="add-dish" href="/users/<%=user._id %>/dishes/new">Add a Dish</a>
                <ul class="contain-cards">
                    <% if (dishes.length===0) { %>
                        <p>There are no items in this collection.</p>
                        <% } else { %>

                            <% dishes.forEach((item)=>{ %>
                                <li>
                                    <a class="user-cards" href="/users/<%= user._id %>/dishes/<%= item._id %>/">
                                        <%= item.name %>
                                    </a>

                                </li>
                                <% }); %>
                </ul>
                <% } %>
            </main>

</body>

</html>